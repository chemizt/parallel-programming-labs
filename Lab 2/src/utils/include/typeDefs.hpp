#include <vector>
#include <chrono>

using std::vector;
using namespace std::chrono;

typedef unsigned int uInt;
typedef vector<vector<int> > intMatrix;
typedef time_point<steady_clock, milliseconds> mSecTimePoint;
